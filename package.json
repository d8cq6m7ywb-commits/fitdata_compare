{
  "name": "fit-file-compare-v2",
  "version": "2.0.0",
  "description": "Compare up to three FIT files from different sensors to diagnose device disagreement",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "dev": "npx http-server . -p 8080 -c-1"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "fit-file-parser": "^2.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "esbuild-plugin-polyfill-node": "^0.3.0"
  }
}
